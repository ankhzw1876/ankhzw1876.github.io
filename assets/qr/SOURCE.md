# Warm-spring QR asset

`warm-spring-profile-qr.jpg` was supplied directly by Wang Tianyu on
2026-09-10 for the XiaHua OS warm-spring sakura window. The file is retained
byte-for-byte at 430×430 pixels, including its center thumbnail.

Chromium's native `BarcodeDetector` decoded the supplied image as:

`http://weixin.qq.com/r/mp/9RM9JQPEX14grUdG90bn`

The asset is enabled only through the internal `qr=profile` launch preset. The
public QR miniature-world generator does not accept arbitrary image paths.
