# XiaHua OS 设计规格

## 本质目标

这不是把现有长页面换一套皮肤，而是把王天宇的个人信息做成一个可探索的桌面系统。访问者主要是招聘者、同行、潜在合作方与对 AI 产品感兴趣的读者，他们通常在笔记本电脑上停留几十秒，也可能从微信或小红书在手机上点开。首要任务是让五类核心信息彼此独立、随点随看，不再沿着长页面滚动查找；其次才是通过“个人 OS”带来记忆点。系统隐喻必须服务信息，不使用虚构的工作经历、技能评分、在线状态、通知或产品数据。

## 叙事与视觉

欢迎页负责建立身份，显示 XiaHua OS、王天宇、AI 产品经理与 Agent/RAG/Evaluation；首屏只预告档案、文章、仓库和内容，不提前宣传游戏数量或具体游戏。点击进入后播放约 3 秒的短开机过程，然后进入桌面。桌面不是空白壁纸，而是保留一句低干扰的核心陈述“把模型能力，做成有人用的产品”，左侧放七个可操作图标。视觉方向采用“暖纸张 × 编辑部工作台 × 原创像素图标”：暖纸色建立个人感，深墨窗口承担内容阅读，锈橙只强调关键句与激活状态；七枚 24×24 像素图标使用同一深墨底座、单色硬边图形和整数网格，统一覆盖桌面、移动 Dock 与任务栏。整体要丰富但不堆叠装饰，所有视觉层次都服务于模块识别与窗口状态。全站字体沿用用户指定的微软雅黑优先字体栈。

## 信息架构

主应用按“关于我”“文章作品”“GitHub”“小红书”“游戏”排列，游戏固定放在小红书之后。辅助应用为“联系我”和“版本归档”。关于我窗口使用“复合身份 → 量化证据 → 个人标签 → 作品入口”的阅读顺序，展示 21 款 App GUI Agent 评测、约 200 条双侧评分、评测数据生产吞吐提升 40%–60% 以及动态原创仓库 / Stars；对外文案不公开内部系统、真实 Query、App 清单、公司与合作方信息。照片保留鼠标跟随 3D 倾斜。文章窗口采用文件列表与预览区，包含三篇已发布文章和一篇草稿；单击只选择，按钮再打开原文。GitHub 窗口展示五个精选仓库、真实项目截图、动态公开仓库数和动态 Stars，API 不可用时保留静态值。小红书窗口展示账号“🍤咀嚼虾滑”与公开主页中 12 篇真实笔记封面，采用中央正面、左右分层透视的 Coverflow，支持侧卡点击、左右按钮、方向键和触屏滑动循环切换。游戏应用首屏是可扩展的文件夹，只陈列真实可运行作品和诚实的预留空位；点击 Joker Card 文件后才延迟加载 iframe，并可返回文件夹。最小化与恢复保留当前游戏进度，关闭窗口或系统重启则回到文件夹并卸载 iframe。播放器提供重新发牌、全屏、独立打开和仓库入口。联系窗口依次提供微信复制、邮箱、电话、GitHub、小红书与文章主页。归档窗口链接到上一版单视图工作台、长滚动版和旧编辑版。

## 交互状态机

桌面图标统一单击打开；触屏沿用同一条 click 路径；键盘依赖原生按钮的 Enter 或 Space 触发。同一应用最多一个窗口，重复打开时恢复并置顶。窗口具有 closed、visible-focused、visible-unfocused、minimized 四种状态；每次变化同步窗口、任务栏、ARIA 和 URL hash。标题栏支持 Pointer Events 拖拽，拖动时完整限制在扣除顶栏与任务栏后的工作区，窗口尺寸变化或旋转屏幕后重新约束。任务栏按钮点击可恢复、聚焦或最小化当前窗口；关闭或最小化前台窗口后，自动激活剩余最高层窗口。窗口只有最小化和关闭，不伪造不可用的最大化与缩放。

## 响应式与无障碍

桌面端固定在 100dvh 内，页面本身不滚，窗口内容独立滚动。手机端保留欢迎和开机，但桌面图标变成两列；应用打开后变成安全区内的全屏面板，不允许拖拽和相互重叠，底部主应用顺序与桌面一致，小红书之后才是游戏。Joker 本体为桌面优先项目，手机端在窗口内给出横屏提示，同时提供全屏和独立打开退路。所有可点击目标至少 44px，窗口标题、控制按钮和任务栏都有明确 aria-label；打开窗口后把焦点放到窗口，进入游戏后聚焦返回文件夹按钮，返回后聚焦原游戏文件，关闭窗口后返回对应图标。非前台、最小化或 hidden/inert 内容不进入键盘顺序。prefers-reduced-motion 下跳过开机等待、窗口弹出与照片倾斜。JavaScript 失败时，通过 noscript 样式把所有应用退化为普通纵向内容，游戏文件本身仍保留可独立打开的 href。

## 约束

不复制 HamstersarusOS 的代码、品牌、紫色 Tokyo Night 外观、仓鼠形象、音乐、图片、小游戏或后端服务。Joker 是用户自有、MIT 许可且独立部署的公开项目，与参考站点的游戏无关。只复现不可版权化的抽象交互模式，并用原创 DOM、CSS、JavaScript 和王天宇现有资料实现。最终需在 2040×1153、1440×900、768×1024、390×844、320×700 视口验证欢迎页、开机、五个主应用、Joker 实际出牌 / 计分、拖拽、最小化、恢复、关闭、键盘与外链行为，控制台不得有错误。
