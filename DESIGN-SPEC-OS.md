# XiaHua OS 设计规格

## 本质目标

这不是把现有长页面换一套皮肤，而是把王天宇的个人信息做成一个可探索的桌面系统。访问者主要是招聘者、同行、潜在合作方与对 AI 产品感兴趣的读者，他们通常在笔记本电脑上停留几十秒，也可能从微信或小红书在手机上点开。首要任务是让五类核心信息彼此独立、随点随看，不再沿着长页面滚动查找；其次才是通过“个人 OS”带来记忆点。系统隐喻必须服务信息，不使用虚构的工作经历、技能评分、在线状态、通知或产品数据。

## 叙事与视觉

欢迎页负责建立身份，显示 XiaHua OS、王天宇、AI 产品经理与 Agent/RAG/Evaluation；首屏只预告档案、文章、仓库和内容，不提前宣传游戏数量或具体游戏。点击进入后播放约 3 秒的短开机过程，然后进入桌面。默认桌面不自动打开任何窗口，保留核心陈述“把模型能力，做成有人用的产品”，左侧放七个可操作图标。「暖春樱花树」保留为独立 LIVE OBJECT 窗口，但不放桌面快捷图标；桌面端可从系统菜单手动打开，也支持显式 `#sakura` 深链接。窗口可拖动、最小化和关闭。视觉方向采用“暖纸张 × 编辑部工作台 × 原创像素图标”：暖纸色建立个人感，深墨窗口承担内容阅读，锈橙只强调关键句与激活状态；像素图标使用同一深墨底座和整数网格，樱花图标只用于任务栏的窗口身份。整体要丰富但不堆叠装饰，所有视觉层次都服务于模块识别与窗口状态。全站字体沿用用户指定的微软雅黑优先字体栈。

## 信息架构

主应用按“关于我”“文章作品”“GitHub”“小红书”“游戏”排列，游戏固定放在小红书之后。辅助应用为“联系我”和“版本归档”。关于我窗口使用“复合身份 → 做事偏好 → 个人判断 → 作品入口”的阅读顺序。右侧不展示履历卡片、项目数字或等分网格，而以自然换行的近直角标签表达任务可验证、首错定位、Judge 边界、Badcase 回归、评分基准、可运行 Demo、原型和内容创作；定量证据留在具体项目材料中，不放进个人介绍。左侧身份标签回答“我是谁”，右侧标签只回答“我怎么做事”，两者不重复。对外文案不公开内部系统、真实 Query、App 清单、公司与合作方信息。照片保留鼠标跟随 3D 倾斜。文章窗口采用文件列表与预览区，包含三篇已发布文章和一篇草稿；单击只选择，按钮再打开原文。GitHub 窗口展示五个精选仓库、真实项目截图、动态公开仓库数和动态 Stars，API 不可用时保留静态值。小红书窗口展示账号“🍤咀嚼虾滑”与公开主页中 12 篇真实笔记封面，采用中央正面、左右分层透视的 Coverflow，支持侧卡点击、左右按钮、方向键和触屏滑动循环切换。游戏应用实际承担 Playground 文件夹的角色，目前包含 Joker Card 与 URL Worlds 两个真实可运行条目，不展示虚构进度或预留位。点击条目后才延迟加载对应 iframe，返回文件夹时焦点回到刚才的条目；最小化与恢复保留当前进度，关闭窗口或系统重启则回到文件夹并卸载 iframe。播放器根据作品动态提供重新载入、全屏、独立打开和仓库入口。联系窗口依次提供微信复制、邮箱、电话、GitHub、小红书与文章主页。归档窗口链接到上一版单视图工作台、长滚动版和旧编辑版。

## URL Worlds 场景规则

URL Worlds 于 2026-09-10 从 URL City 升级为真实 3D 实验，保留 `experiments/qr-skyline/` 目录与旧访问地址。以“输入链接 → 选择风景 → 旋转观察 → 俯视扫码”为最短体验路径，提供城市、树林、山地、雪地、沙漠、塔楼六种场景。场景是视觉主体，控制区只承担链接生成、主题切换、俯视切换、角度调节与自动环绕，不展示无助于体验的实现指标。

每个 QR 模块对应固定 XZ 方格，黑格物体和浅色地面保留俯视时的深浅关系及四模块静区；地形场景通过共享角点高度形成连续山脊和沙丘。同一链接、同一场景在观察过程中保持几何与颜色不变，仅旋转正交相机并调整取景范围，最终从正上方直接看到二维码。更换链接或场景才重新生成几何。俯视取景同时对齐整数物理像素模块大小与网格原点，最后 15°平滑过渡取景范围，避免扫码模糊和终点缩放跳变。旧 URL City v1 的“二维码列映射楼宇、黑格映射窗灯，再重排窗灯”属于已替换的 Canvas 2D 机制，不再作为当前交互描述。

## 交互状态机

桌面图标统一单击打开；触屏沿用同一条 click 路径；键盘依赖原生按钮的 Enter 或 Space 触发。同一应用最多一个窗口，重复打开时恢复并置顶。窗口具有 closed、visible-focused、visible-unfocused、minimized 四种状态；每次变化同步窗口、任务栏、ARIA 和 URL hash。标题栏支持 Pointer Events 拖拽，拖动时完整限制在扣除顶栏与任务栏后的工作区，窗口尺寸变化或旋转屏幕后重新约束。任务栏按钮点击可恢复、聚焦或最小化当前窗口；关闭或最小化前台窗口后，自动激活剩余最高层窗口。窗口只有最小化和关闭，不伪造不可用的最大化与缩放。

## 响应式与无障碍

桌面端固定在 100dvh 内，页面本身不滚，窗口内容独立滚动。手机端保留欢迎和开机，但桌面图标变成两列；应用打开后变成安全区内的全屏面板，不允许拖拽和相互重叠，底部主应用顺序与桌面一致，小红书之后才是游戏。Joker 本体为桌面优先项目，手机端在窗口内给出横屏提示，同时提供全屏和独立打开退路。URL Worlds 面向 320px 起的独立与 iframe 视口设计；prefers-reduced-motion 下直接切换相机终态，不启动自动环绕。无 WebGL 时保留标准二维码降级路径。文档或父播放器隐藏时暂停渲染，恢复可见后继续；关闭窗口或返回文件夹仍按播放器规则卸载 iframe。高密度 QR 省略城市窗户细节，塔楼最多三层，限制移动设备的几何负担。所有可点击目标至少 44px，窗口标题、控制按钮和任务栏都有明确 aria-label；打开窗口后把焦点放到窗口，进入游戏后聚焦返回文件夹按钮，返回后聚焦原游戏文件，关闭窗口后返回对应图标。非前台、最小化或 hidden/inert 内容不进入键盘顺序。prefers-reduced-motion 下也跳过开机等待、窗口弹出与照片倾斜。JavaScript 失败时，通过 noscript 样式把所有应用退化为普通纵向内容，游戏文件本身仍保留可独立打开的 href。

## 约束

不复制 HamstersarusOS 的代码、品牌、紫色 Tokyo Night 外观、仓鼠形象、音乐、图片、小游戏或后端服务。Joker 是用户自有、MIT 许可且独立部署的公开项目，与参考站点的游戏无关。URL Worlds 使用原创场景几何和交互，以本地 vendored Three.js 0.160.1 实现 WebGL 真实 3D，以 qrcode-generator 1.4.4 生成 UTF-8、M 级纠错二维码；两者均保留 MIT 许可。它不是 WebGPU 实现，也不复用 Every QR Code 的代码和素材。实验零构建、无 CDN 依赖，直接通过 `file://` 打开即可运行。链接限 HTTP(S)，最多键入 512 个字符，规范化后最多 1,800 个字符，非法协议与超容量输入必须给出明确提示。

最终需在 2040×1153、1440×900、768×1024、390×844、320×700 视口验证欢迎页、开机、五个主应用、Joker 实际出牌 / 计分、拖拽、最小化、恢复、关闭、键盘与外链行为，控制台不得有错误。URL Worlds 还需覆盖无协议网址、主机加端口、中文与 emoji URL、非法协议拒绝、同一规范化 URL 的结构确定性、不同 URL 的差异、六种场景的二维码解码、相机切换前后的几何与颜色不变、iframe 返回与焦点恢复、隐藏暂停及 WebGL 降级。

## URL Worlds 当前验收记录

2026-09-10 已完成默认主页 URL 在六种场景下的桌面、390×844、320×700 自动二维码解码，手机布局无横向溢出；中文和 emoji、非法协议拒绝、主机加端口的输入验证通过。153×153 长中文 URL 的六种场景均在 320×700、DPR 1 下通过自动解码。斜视与俯视之间的几何 positions、colors 校验和完全一致。新版主页 iframe 在 1440×900 桌面父视口与 320×700 手机父视口中扫码通过，手机内页无横向溢出；返回文件夹暂停并恢复卡片焦点，再次打开恢复，关闭卸载。实体手机实扫仍待完成，与浏览器自动解码分开记录。

## 二维码微缩世界更新记录

2026-09-10，游戏文件夹第二项改接 `experiments/qr-sakura/` 的“二维码微缩世界”。入口提供樱花树、浮空群岛、月球基地、微缩书城四个确定性世界；共用同一 URL、QR 基础矩阵、四模块静区、配色、相机和拖拽复位逻辑。切换世界只更新本地程序化几何缓冲，不重建设备或重算二维码。旧目录、旧地址保持不变，新页页脚提供“六种风景旧版”入口；这个通用入口仍作为 Playground 文件存在，后续另将暖春樱花的定制展示纳入顶层窗口。

同日，暖春樱花树改为独立顶层应用，后续取消了默认启动。无 Hash 进入只显示空闲桌面；只有桌面端系统菜单、显式 `#sakura` 深链接或站内调用才会打开该窗口。打开后以 `embed=1&palette=spring&world=sakura&target=wechat` 启动；其他深链接仍只打开各自指定的应用。非前台或最小化时暂停，关闭时卸载；左侧圆点与右上角按钮均可关闭，与其他 XiaHua OS 窗口一致。`target=wechat` 是内部白名单 URL 预设，直接让 3D 树形与原生二维码编码 `http://weixin.qq.com/r/mp/9RM9JQPEX14grUdG90bn`。不再使用粉色截图作为窗口占位，也不再在变形终点覆盖用户提供的二维码图片；普通 Playground 入口的自由 URL 和四个世界不受影响。

樱花树仍按[小红书视频](https://www.xiaohongshu.com/explore/6a9538da000000001f004395)的主体关系实现：单棵粉色樱花树、弯曲主干与分叉、草边棋盘石台。三个新增题材使用轮廓明确的低面数几何，不依赖外部模型或粒子：浮岛强调上宽下尖与瀑布，月站强调低矮圆顶与太阳能翼，书城强调书架围合与中央阅读桌。首要体验变为“输入链接 → 选择世界 → 点击收束成彩色 QR → 再次点击返回”。

过渡同时改变镜头、模型形态和材质，枝叶随俯视过程收束，花草和地面颜色构成二维码；不能描述成旧版的“固定模型仅转镜头”。链接决定树形，当前生成配方下同一规范化 URL 保持确定性，不同 URL 参与生成不同结构，但不保证外观绝不重复。花瓣以粉色为主体，树干保留棕色，草边与 QR 的绿色模块建立视觉联系；扫码终态的对比度需与树景的柔和色彩分别调校。

新页基于 MIT 许可的 [Every QR Code](https://github.com/AlbertAZ1992/every-qrcode) 源码改造，固定上游提交 `ed404c6cba9d48c04d5e08de780293cff1b242de`，本地视觉配方为 `micro-worlds-study-2`。许可与依赖声明分别保留在 `engine-src/upstream/LICENSE` 和 `vendor/THIRD_PARTY_LICENSES.txt`。视频参考与开源代码来源分别署名，未确认该仓库就是视频作者的源码，不承诺逐像素复现。上文“原创 WebGL 几何、未复用 Every QR Code”的约束仅适用于旧 URL Worlds。

完整效果使用 WebGPU，需要 HTTPS 或 localhost 与可用适配器；依赖本地打包，页面运行时不需 CDN、外部模型或服务端处理链接。无 WebGPU、初始化失败或设备丢失时保留标准高对比二维码。减少动态效果时冻结环境动画并立即切换终态；页面、舞台或同源父播放器隐藏时暂停。输入只接受 HTTP(S)，拒绝账号密码；除字符上限外，完整引擎受上游 QR version 6 容量约束，长链接提示改用短链接，不能继承旧六场景版的高密度容量承诺。

本版四个世界均在 1200×900 真实 WebGPU 截图中通过原生 `BarcodeDetector` 解出默认主页链接；390×844 无横向溢出且成功解码。浮岛完成真实拖拽和松手复位检查，书城转换中间帧未发现孤立白色散点。主页 iframe、暂停恢复、减少动态效果与无 WebGPU 降级沿用并通过原有检查。WebGPU canvas 提交后直接读取可能取得清空帧，视觉与扫码检查以真实浏览器截图为准。实体手机相机实扫尚未验证。
